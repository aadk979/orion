// The one line to edit when deploying elsewhere (e.g. an EC2 public IP):
const SERVER_URL = 'http://localhost:3900';

// Local dev note: the static client is served on 8081, not 8080 — see README.

export { SERVER_URL };
