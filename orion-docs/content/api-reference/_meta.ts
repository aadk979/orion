import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  index: "Conventions",
  authentication: "Authentication endpoints",
  account: "Account & 2FA endpoints",
  sessions: "Session endpoints",
  errors: "Error codes",
  "admin-api": "Orchestrator admin API",
};

export default meta;
