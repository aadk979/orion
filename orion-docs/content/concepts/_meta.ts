import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  index: "Overview",
  packages: "The four packages",
  "request-lifecycle": "Request lifecycle",
  "sessions-and-tokens": "Sessions & tokens",
  "security-tiers": "Security tiers",
  "cluster-model": "Cluster model",
};

export default meta;
