import type { MetaRecord } from "nextra";

const meta: MetaRecord = {
  index: "Overview",
  reference: "API reference",
  flows: "Interruption flows",
  "building-the-sdk": "Building the SDK",
};

export default meta;
